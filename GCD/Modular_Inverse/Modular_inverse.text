a의 N에 대한 모듈러 곱셈의 역원

x 가 a의 N에 대한 모듈러 곱셈의 역원
ax === 1 (mod N)
두 항을 모두 나머지 연산할 때 같은 값이 나온다는 의미
즉 ax % N = 1 과 같다

ax = kN + 1 이라고 볼 수 있고 식을 조금 수정하면
ax + Ny = 1
이 식이 정수해 (x, y)를 가질 조건은 a, N 이 서로소
즉 a 와 N 이 서로소일 때만 모듈러 곱셈 역원 구할 수 있음

a, N 에 대한 (x, y) 정수해를 EED 를 이용하여 x를 구한다
이후 x값을 N 안에 들어가게끔 맞춘다